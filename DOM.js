// find you button that you want to apply the event listener to.

const searchButton = document.getElementsByClassName(
  "searchEngine__buttons__search-button"
);

searchButton.addEventListener("click", () => {
  // when it is clicked have the search of the API start
  // numberOne += numbersArr[i];
  // Then have the answers display in the object grid.
  // document.getElementById("number-display").innerText = numberOne;
});
